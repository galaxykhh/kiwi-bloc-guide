part of '../screen/infinite_scroll_screen.dart';

class _InfiniteScrollErrorView extends StatelessWidget {
  const _InfiniteScrollErrorView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('에러남ㅋ'));
  }
}
